## Installation

```
git clone https://github.com/k-onoue/mostly-harmless-approximation.git
cd mostly-harmless-approximation
pyenv local 3.12
source .venv-42/bin/activate
pip install --upgrade pip
pip install -r requirements.txt
pip install -e .
```