## Installation

```
git clone https://github.com/k-onoue/closedform-openmind.git
cd closedform-openmind
pyenv local 3.12
source .venv/bin/activate
pip install --upgrade pip
pip install -r requirements.txt
pip install -e .
```

